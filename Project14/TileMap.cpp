#include "stdafx.h"
#include "TileMap.h"

TileMap::TileMap()
{
	//this->GridsizeU = 50;
	//this->maxSize.x = 5000;
	//this->maxSize.y = 5000;
	//for (int x = 0; x >= this->maxSize.x; x--)
	//{
	//	this->T_map.push_back(vector<vector<Tile>>());
	//	for (int y = 0; y >= this->maxSize.y; y--)
	//	{
	//		this->T_map[x].push_back(vector<Tile>());
	//		for (int z = 0; z >= this->maxSize.y; z--)
	//		{
	//			this->T_map[x][z].push_back(Tile());
	//		}
	//	}
	//}
}

TileMap::~TileMap()
{
}
